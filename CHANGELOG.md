# ArtForge V4.0 — تغییرات تا این مرحله

## فاز ۱ — بک‌اند پایه ✅ تکمیل و تست‌شده
`server/` — سرور API با Auth واقعی (JWT + scrypt)، دیتابیس SQLite، RBAC پایه.
جزئیات کامل: `server/README.md`

## فاز ۲ — اتصال فرانت‌اند موجود به بک‌اند ✅ تکمیل و تست‌شده

بدون تغییر ظاهر یا ساختار صفحات موجود — فقط اتصال داده اضافه شد:

| فایل | تغییر |
|---|---|
| `js/modules/api.js` **(جدید)** | لایهٔ ارتباط با API — اگر سرور در دسترس نباشد، بی‌صدا fail می‌شود و UI دموی محلی دست‌نخورده کار می‌کند |
| `js/modules/store.js` | ۵ تابع جدید اضافه شد (`registerRemote`, `loginRemote`, `logoutRemote`, `syncOrdersFromServer`, `addOrderSynced`) — همهٔ توابع قبلی بدون تغییر باقی ماندند |
| `signup.html` | اگر ایمیل معتبر وارد شود و رمز عبور ≥ ۸ کاراکتر باشد و سرور در دسترس باشد → ثبت‌نام/ورود واقعی؛ در غیر این صورت رفتار دموی قبلی (بدون تغییر) |
| `pages/new-order.html` | ثبت سفارش هم‌زمان محلی (فوری، بدون تغییر UX) و ارسال پس‌زمینه به سرور |
| `pages/dashboard.html` | پس از بارگذاری، سفارش‌های واقعی از سرور می‌آیند و داشبورد دوباره رندر می‌شود |
| `pages/orders.html` | همان — سینک real و رندر مجدد |
| بقیهٔ صفحات (`admin.html`, `designer.html`, `pages/*.html`) | فقط `api.js` include شد تا در دسترس باشد؛ منطق داخلی تغییر نکرد |

### تست انجام‌شده (end-to-end، نه فقط نوشتن کد)
با اجرای واقعی سرور و شبیه‌سازی دقیق همان فراخوانی‌هایی که مرورگر انجام می‌دهد:
ثبت‌نام → ثبت سفارش → سینک لیست سفارش‌ها → خروج و ورود دوباره (شبیه بازدید بعدی) → سفارش هنوز
هست. همه با موفقیت تایید شد.

### رفتار در حالت آفلاین
اگر `server/` اجرا نشده باشد، هیچ صفحه‌ای خراب نمی‌شود — همه‌چیز دقیقاً مثل قبل (v3.0) روی
`localStorage` کار می‌کند. این عمدی است: در فاز ۲ چیزی حذف نشد، فقط لایهٔ sync واقعی اضافه شد.

## قدم بعدی (فاز ۳)
Kanban / Workspace واقعی روی دیتابیس (چت، فایل، تایم‌لاین به‌جای دادهٔ seed محلی).
